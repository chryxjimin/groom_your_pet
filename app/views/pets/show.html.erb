<h1><%= @pet.name %></h1>

Age: <%= @pet.age %></br>
Breed: <%= @pet.breed %></br>
Weight: <%= @pet.weight %></br>
</br>

<%= link_to @pet.owner.name, owner_path(@pet.owner) %></br>
<%= link_to 'Delete',url_for(action: :destroy, id: @pet.id),method: :delete, data: {confirm: "Are you sure?"} %></br>
<%= link_to "Edit Pet", edit_owner_pet_path(@owner, @pet) %></br>
<%= link_to "Create a new appointment", new_appointment_path %>

<%# #added line 8 %>