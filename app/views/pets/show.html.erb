<h1><%= @pet.name %></h1>

Age: <%= @pet.age %></br>
Breed: <%= @pet.breed %></br>
Weight: <%= @pet.weight %></br>
</br>

<%= link_to 'Delete',url_for(action: :destroy, id: @pet.id),method: :delete, data: {confirm: "Are you sure?"} %></br>
<%= link_to "Edit Pet", edit_pet_path(@pet) %></br>
<%= link_to "Create a new appointment", new_groomer_appointment_path(current_groomer) %>